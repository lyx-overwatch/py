目标

- 学习 Docker、Docker Compose 与 GitHub Actions 的基本部署流程

练习题

1. 为之前的 FastAPI 项目写 Dockerfile 与 docker-compose.yml。
2. 编写 GitHub Actions workflow 在 push 时运行测试并构建镜像。
3. 在本地使用 docker-compose 启动服务并验证。

交付物

- Dockerfile 模板 + docker-compose.yml 示例
- .github/workflows/basic.yml 示例
